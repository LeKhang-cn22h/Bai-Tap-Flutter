import 'package:flutter/material.dart';

class Week1Name extends StatelessWidget {
  const Week1Name({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(

    "Lê Tuấn Khang",
      style: TextStyle(
        fontSize: 24,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
